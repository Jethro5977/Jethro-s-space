{
  "name": "card-builder",
  "version": "1.0.0",
  "private": true,
  "description": "DIY 3D sports card builder with a Three.js acrylic slab preview",
  "scripts": {
    "start": "python3 -m http.server 4174 --bind 127.0.0.1",
    "check": "node --check app.js && node --check three-preview.js && node --check scripts/audit-player-data.mjs",
    "audit:players": "node scripts/audit-player-data.mjs"
  },
  "dependencies": {
    "three": "0.185.1"
  }
}
