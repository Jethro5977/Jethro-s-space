# Card Builder checkpoint — 2026-09-05

## 当前保存点

- 分支：`codex/card-builder-renderer-package`
- 范围：renderer 公共 API 拆分、source/ESM/UMD 三入口契约、captureCanvas 生命周期修复，以及本机 esbuild Darwin arm64 修复。
- 本地应用仍通过 import map 直接使用 `packages/renderer/src/index.js`；npm 包入口使用构建后的 `dist/card-renderer.esm.js` / `dist/card-renderer.umd.js`。
- UMD 产物现在携带其内嵌 Three.js addon 的 MIT 许可说明。

## 已验证

- `npm run test:renderer`：46/46 通过。
- `npm run test:renderer:browser`：10/10 通过，覆盖 source、npm ESM、UMD、真实 WebGL、高清导出、partial state/view、flip、多实例隔离、canvas ownership 和坏图 fallback。
- esbuild：官方安装脚本已恢复本机 `darwin-arm64` 二进制；未改写 lockfile。
- 旧 export visual baseline 已完成审阅；八种 effect 的 working tree 与 HEAD 图像一致，旧 hash 文件已更新为当前已核验基线。

## 下一步

- 本 checkpoint 提交并部署到现有 Vercel 项目 `cardsbuilder`，随后重新检查 production 页面与 `/api/health`。
- GitHub 组件选型已记录，当前选择 `catdad/canvas-confetti@1.9.4`（ISC）；本次部署不接入，避免将新视觉变量与 renderer API 保存点混合。
- 部署完成后再进入 confetti pack reveal 的实际视觉优化、截图和回归测试。
